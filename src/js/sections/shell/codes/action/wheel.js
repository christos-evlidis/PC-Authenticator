import { codesAnimationWheelStart } from "../animation/wheel/start.js";

/** Registers wheel scrolling for the codes list. */
function _codesActionWheel() {
  codesAnimationWheelStart();
}

export { _codesActionWheel as codesActionWheel };
